spring:
  application:
    name: appointment-service

  config:
    import: "optional:configserver:"

  cloud:
    config:
      uri: ${CONFIG_SERVER_URIS}

server:
  port: 8083   # <-- මෙතන port එක assign කරලා තියෙන්න

eureka:
  instance:
    hostname: appointment-service-a.platform
    prefer-ip-address: false
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URLS}